-- Name : Yiyang Lin
-- Student ID: 9080288724

SELECT
	S_NAME, count(DISTINCT O_CUSTKEY)
FROM
	LINEITEM,
	ORDERS,
	SUPPLIER
WHERE
	L_ORDERKEY = O_ORDERKEY
	AND S_SUPPKEY = L_SUPPKEY
			
GROUP BY L_SUPPKEY
HAVING count(DISTINCT O_CUSTKEY) > 615
ORDER BY count(DISTINCT O_CUSTKEY) DESC